apiVersion: v1
kind: Secret
metadata:
  name: mongodb-secret
  namespace: tech-challenge
type: Opaque
stringData:
  # These are example values for demonstration
  # In production, use sealed-secrets, external-secrets, or AWS Secrets Manager
  # IMPORTANT: Never commit real credentials to git
  mongodb-root-username: admin
  mongodb-root-password: CHANGE_ME_IN_PRODUCTION
  mongodb-uri: mongodb://admin:CHANGE_ME_IN_PRODUCTION@mongodb:27017/tech_challenge?authSource=admin
